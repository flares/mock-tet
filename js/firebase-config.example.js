// Firebase project config — copy this file to js/firebase-config.js (which is gitignored)
// and fill in your real values. NEVER commit firebase-config.js to git.
//
// Setup steps:
//   1. Go to Firebase console → Mana TET project → Project settings
//      → Your apps → Add app (Web) → copy the firebaseConfig object below.
//   2. In Firebase console, go to Build → AI → Enable Gemini Developer API.
//   3. Copy THIS file to js/firebase-config.js alongside it and paste real values.
//
window.FIREBASE_CONFIG = {
  apiKey:            "YOUR_FIREBASE_API_KEY",
  authDomain:        "your-project-id.firebaseapp.com",
  projectId:         "your-project-id",
  storageBucket:     "your-project-id.firebasestorage.app",
  messagingSenderId: "000000000000",
  appId:             "1:000000000000:web:0000000000000000000000",
};
